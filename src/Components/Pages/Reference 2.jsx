import SectionContent from '../SectionContent/default';
import data from '../../Data/menu.json';

function Reference() {
    return <SectionContent section={data.reference} />;
}

export default Reference;