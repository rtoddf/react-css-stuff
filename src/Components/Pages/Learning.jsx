import SectionContent from '../SectionContent/default';
import menuData from '../../Data/menu.json';

function Learning() {
    return <SectionContent section={menuData.learning} />
}

export default Learning;